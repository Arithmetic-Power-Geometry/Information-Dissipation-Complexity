# Root-count frontier: next-stage attack from the certified five-root family

## Current certified frontier

[
N_{max}(1)=0,qquad N_{max}(2)=1,qquad N_{max}(3)=3,
]
and
[
oxed{5le N_{max}(4)le11}.
]

The four-level lower bound is witnessed by
[
x=(0,7/250,57/400,137/500),qquad r=(1,103,5246,6376),
]
with five disjoint interval-certified sign-changing brackets for (kappa_3(t)) on (t>1).

## Two mechanisms that can change the physical root count

A continuation restricted to the physical half-line (t>1) must track two distinct codimension-one mechanisms.

### 1. Interior pair creation/annihilation
An interior multiple root satisfies
[
kappa_3(t_*)=0,qquad kappa_4(t_*)=0,qquad t_*>1.
]
If (kappa_5(t_*)
e0), this is a nondegenerate quadratic tangency. A transverse crossing generically changes the physical root count by two.

### 2. Boundary entry/exit
A simple root may cross the physical boundary at (t=1). The boundary surface is
[
kappa_3(1)=0.
]
If (kappa_4(1)
e0), the crossing is transverse in (t) and can change the physical root count by one.

This second mechanism is essential when moving from an odd count such as five to an even count such as six. It must therefore be searched alongside interior double-root loci.

## Scale-aware parameterization

Write
[
x=a(0,1,u,v),qquad a>0,quad1<u<v.
]
For physical-root searches the scale (a) is retained. A common additive shift of all information levels is harmless, but fixing (a=1) is not harmless because it moves zeros relative to (t=1).

## Promotion protocol

A numerical family with at least six sign-changing roots is only a candidate. Promotion requires:

1. high-precision root refinement;
2. rational approximation of all information gaps;
3. integerization of multiplicity ratios;
4. recounting after simplification;
5. disjoint outward-rounded interval sign certificates;
6. only then update the theorem ledger.

Until that chain succeeds, the theorem remains
[
oxed{5le N_{max}(4)le11}.
]
