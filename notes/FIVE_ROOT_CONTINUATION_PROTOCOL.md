# Five-root continuation protocol

The certified five-root family is now the center of the search rather than a random global seed.

Seed:
[
x=(0,7/250,57/400,137/500),qquad r=(1,103,5246,6376).
]

The continuation retains all three absolute information gaps, so the physically meaningful boundary (t=1) is respected. Random anchors perturb the three gaps and three log multiplicity ratios. A nonlinear solve seeks
[
kappa_3(t_*)=kappa_4(t_*)=0,qquad t_*>1.
]
Nondegenerate candidates require (kappa_5(t_*)
e0).

Each accepted bifurcation is crossed in both directions along every gap/multiplicity coordinate. The complete physical root count is recomputed on a mixed grid extending to (t=500).

Promotion rule:
- <=5 roots: exploratory continuation evidence only;
- >=6 roots: save exact numerical candidate, rationalize gaps, integerize multiplicities, recount at high precision;
- only after outward-rounded interval certificates on disjoint brackets may the manuscript lower bound be strengthened.

The current certified theorem remains (5le N_{max}(4)le11) until that promotion chain succeeds.
