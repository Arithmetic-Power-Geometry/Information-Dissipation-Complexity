# C=11 constrained search protocol

The four-level chamber certificate contains exactly four generic chambers with maximum coefficient sign variation (mathcal C=11). This experiment restricts geometry to those four chambers and optimizes actual physical sign-changing zeros of (kappa_3(t)) on (t>1).

The ten affine wall signs are imposed as hard chamber-membership constraints. Each chamber receives 12 independent differential-evolution restarts, with three free log multiplicity ratios and two positive gap coordinates. Root counting uses a mixed dense grid to (t=250).

A count above four is only a discovery candidate. Promotion requires high-precision refinement, rational/integer replacement, and outward-rounded interval sign certificates. Failure to exceed four is negative computational evidence only.

This experiment is deliberately separated from the theorem ledger so the current certified statement (4le N_{max}(4)le11) cannot be accidentally strengthened by optimization output.
